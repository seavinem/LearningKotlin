/* Вывести на экран большее из двух целых чисел. */

fun main(){
    val a = readln().toInt()
    val b = readln().toInt()

    if (a > b ) {
        println(a)
    }
    else {
        println(b)
    }

    var x = mutableListOf<Int>()
    x.size
    for(i in 0..x.size) {
        x.remove(4)
    }
}